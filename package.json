{
  "scripts": {
    "dev": "npx tailwindcss -i ./src/styles.css -o ./public/dist/output.css --watch",
    "build": "mkdir -p public/dist && cp -r src/* public/ && npx tailwindcss -i ./src/styles.css -o ./public/dist/output.css"
  },
  "dependencies": {
    "marked": "^15.0.10"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "autoprefixer": "^10.4.14",
    "tailwindcss": "^3.3.0"
  }
}
